const backGroundModel = require('../models/backgroundModel');

// upload data to mongoDB
async function uploadBackgroundData (allTombsDict) {

};

// get req with puppeteer by year
async function getBackgroundData (year) {

};

// get req with mongoDB by year
async function getMongoDB (year) {

};

// compare the two
async function compareTheTwo () {

};

module.exports = {
    uploadBackgroundData,
    getBackgroundData,
    getMongoDB,
    compareTheTwo
}